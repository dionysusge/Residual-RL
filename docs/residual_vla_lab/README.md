# pi0.5 explicit residual v0

This implementation intentionally stops at research-readiness smoke scope. The
two-GPU topology is documented in `ARCHITECTURE.md`; CPU replay and distributed
boundaries are in `COMMUNICATION_BOUNDARY.md`; server commands and conservative
32/64-env profiling are in `ENVIRONMENT.md`. The authoritative status and all
deliberately unclaimed GPU gates are in `RESEARCH_READINESS_REPORT.md`.

